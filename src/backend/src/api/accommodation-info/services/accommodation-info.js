'use strict';

/**
 * accommodation-info service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::accommodation-info.accommodation-info');
