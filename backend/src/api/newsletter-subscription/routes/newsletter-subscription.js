'use strict';

/**
 * newsletter-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::newsletter-subscription.newsletter-subscription');
