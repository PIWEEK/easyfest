{
  "kind": "singleType",
  "collectionName": "sites",
  "info": {
    "singularName": "site",
    "pluralName": "sites",
    "displayName": "site",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "registration": {
      "type": "enumeration",
      "enum": [
        "hidden",
        "soon",
        "open",
        "finished"
      ],
      "default": "hidden",
      "required": true
    },
    "agenda": {
      "type": "enumeration",
      "enum": [
        "hidden",
        "soon",
        "draft",
        "final"
      ],
      "default": "hidden",
      "required": true
    },
    "eventStatus": {
      "type": "enumeration",
      "enum": [
        "hype",
        "public",
        "finished"
      ],
      "default": "hype",
      "required": true
    },
    "loginEnabled": {
      "type": "boolean"
    }
  }
}
